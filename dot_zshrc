export PS1='$(date +"[%H:%M]") %f%F{cyan[bold]}%2~%f%F{yellow}$(git_branch_name)%f%F{magenta}%#%f '

ZSH_CONF_DIR="${HOME}/.config/zsh"

if [ -d "${ZSH_CONF_DIR}" ]; then
	for file in "${ZSH_CONF_DIR}"/*.zsh; do
		source "${file}"
	done
fi

